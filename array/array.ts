const heros:object[]=[]
heros.push({name:'barbod'})

const barbodHero:string[]=[]
barbodHero.push('barbod')

const newArray:Array<number>=[]
newArray.push(2)

type User={
    username:string
    password:number
}

const users:Array<User>=[]
users.push({username:'',password:3})








export {}