enum SeatChoice {
    FIRST,//default start the number of first variable is zero (0) if you want change the start number just do this FIRST = 10 
    WINDOW,
    MIDDLE,
}

const locSeat = SeatChoice.WINDOW

export {}